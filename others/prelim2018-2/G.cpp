#include <bits/stdc++.h>

using namespace std;

int main() {
  int N, B;

  while(cin >> N >> B, N || B) {
    //?

    while(B--) {
      int i, j, t;
      cin >> i >> j >> t;
      //?
    }
  }
}
