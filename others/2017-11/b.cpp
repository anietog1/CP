#include <iostream>
#include <algorithm>

using namespace std;
#define MAXN 100010
#define d(x) cout << #x << " = " << x << endl;

int n, p[MAXN], c[MAXN];

int fact(int n){
  
}

int main(){
  cin >> n;
  
  for(int i=0;i<n;++i)
    cin >> p[i];
  
  for(int i=0;i<n;++i)
    cin >> c[i];

  sort(p, p + n);
  sort(c, c + n);
  
  
}
